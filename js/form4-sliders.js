// This function updates the value of an input element with the given ID.
function outputUpdate(inputId, outputValue) {
  // Get the input element with the given ID and set the value of the input element to the given output value.
  document.querySelector('#' + inputId).value = outputValue;
}
